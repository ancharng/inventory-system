import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import InventoryCheckingSystem from './InventoryCheckingSystem';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <InventoryCheckingSystem />
  </React.StrictMode>
);
